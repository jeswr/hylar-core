on: pull_request

jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github.event.pull_request.labels) }}
        run: echo "$GITHUB_CONTEXT" && echo contains($GITHUB_CONTEXT, "automerge")
